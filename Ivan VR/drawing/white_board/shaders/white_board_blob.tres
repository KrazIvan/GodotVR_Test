[gd_resource type="Shader" format=3 uid="uid://cqddoeu8fhqwr"]

[resource]
code = "shader_type canvas_item;
render_mode blend_disabled;


uniform sampler2D SCREEN_TEXTURE : hint_screen_texture, filter_linear_mipmap;
uniform float inner_radius : hint_range(0.0, 0.5, 0.01) = 0.45;

void fragment() {
	float dist = distance(
		UV, 									
		vec2(0.5, 0.5));						

	float m = smoothstep(
		inner_radius, 							
		0.5, 									
		dist);									

	COLOR = mix(
		texture(SCREEN_TEXTURE, SCREEN_UV), 	
		COLOR, 									
		1.0 - m);								
}"
